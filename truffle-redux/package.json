{
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "compile": "cd contracts && npm run build",
    "migrate": "cd contracts && npm run deploy",
    "start": "cd web && npm start",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.0.0"
  },
  "dependencies": {
    "emotion": "^9.2.10",
    "react-bootstrap": "^0.32.4",
    "react-loader-spinner": "^2.0.6",
    "react-spinners": "^0.4.5"
  }
}
